# 모듈 불러올때 import 모듈 이름 or 모듈내 함수만 불러오고 싶을 경우 from 모듈이름 import 모듈 함수

# if__name__=="__main__"

# sys : 파이썬 설치시 함께 설치되는 라이브러리 모듈
# sys.path : 파이썬 라이브러리가 설치된 디렉터리 목록 보여줌 이 안에 저장된 모듈은 디렉터리 이동할 필요없이 바로 불러서 사용 가능
# append(추가)해서 사용 가능